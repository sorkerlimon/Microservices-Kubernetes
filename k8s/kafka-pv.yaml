apiVersion: v1
kind: PersistentVolume
metadata:
  name: kafka-pv
  namespace: kub-project
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/kafka
