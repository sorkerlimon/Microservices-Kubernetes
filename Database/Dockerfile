# Use the latest official MySQL image
FROM mysql:latest

# Set environment variables (change values as needed)
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=kubdb
ENV MYSQL_USER=kubuser
ENV MYSQL_PASSWORD=kubpassword

# Expose MySQL port
EXPOSE 3306

# By default, MySQL runs on container start
